import React from "react";

const SearchPatient = () => {
  return (
    <div>
      <input
        type="text"
        name="Search"
        placeholder="Search..."
        autoComplete="off"
      />
    </div>
  );
};

export default SearchPatient;
